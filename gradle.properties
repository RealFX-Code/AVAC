# Gradle Properties
org.gradle.jvmargs = -Xmx1G -XX:MaxMetaspaceSize=1G
org.gradle.parallel = true

# Mod Properties
version = develop
maven_group = rocks.realfx
archives_base_name = avac

# Dependencies are managed at gradle/libs.versions.toml
